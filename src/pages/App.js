import * as React from"react";import{Route,Switch,BrowserRouter}from'react-router-dom';import ReactDOM from'react-dom';import Home from'./Home';import Dao from'./Dao';import slugify from'../functions/slugify.js';var{daos}=require('../data/daos.js');export default function App(){return(<a><BrowserRouter><Switch><Route exact path='/'><Home/></Route>{daos.map((element,index)=>{return(<Route exact path={`/${slugify(element.daoName)}`}><Dao element={element}/></Route>)})}</Switch></BrowserRouter></a>);}